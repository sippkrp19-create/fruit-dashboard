# เก็บข้อมูลเบื้องลึก DashBoard

วันที่บันทึก: 2026-05-27

โฟลเดอร์นี้ใช้เก็บข้อมูลเบื้องหลังของงานแดชบอร์ดสวนผลไม้ เพื่อให้กลับมาแก้ต่อได้ง่าย หรือให้ AI อ่านต่อแล้วเข้าใจภาพรวมทันที

## Google Sheets ล่าสุด

ลิงก์ไฟล์ Google Sheets ที่ผู้ใช้ส่งล่าสุด:

https://docs.google.com/spreadsheets/d/1YEsURhIz4oyM9sVebBrXfHId9TRn1BKOVEQVIqvVBHA/edit?usp=sharing

ลิงก์ Google Sheets เดิมที่เคยคุย:

https://docs.google.com/spreadsheets/d/1wqsqJkXGchsUCg2mgY06qjKQLoZR4wBLL4f3fjJc-Uc/edit?usp=sharing

## ไฟล์ Excel ต้นทาง

ไฟล์ Excel หลักในเครื่อง:

`C:\Users\sippk\OneDrive\เอกสาร\Open Design\dashboard\สวนผลไม้_บัญชี_สรุปมีรายรับ.xlsx`

โฟลเดอร์สคริปต์:

`C:\Users\sippk\OneDrive\เอกสาร\ทำเว็ปเพจ\แดชบอร์ด`

## โครงสร้างชีตหลัก

- `เพิ่มข้อมูล`
- `ฐานข้อมูล`
- `สรุป`
- `ต้นฉบับ`
- `รายการดรอปดาวน์` เพิ่มในไฟล์ Excel เพื่อให้แก้ dropdown ง่าย

## หัวตารางฐานข้อมูล

ชีต `ฐานข้อมูล` ใช้หัวตาราง 18 คอลัมน์:

1. วันที่
2. รอบ/ฤดูกาล
3. สินค้า
4. ประเภทรายการ
5. สายพันธุ์/เกรด
6. ชื่อ/รายการ
7. ราคาต่อกก.
8. ปริมาณกก.
9. รายรับ
10. รายจ่าย
11. ค่าตัด
12. เบิก
13. สุทธิ
14. วิธีรับ/จ่ายเงิน
15. สถานะรายการ
16. วันที่บันทึก
17. หมายเหตุ
18. แหล่งที่มา

## การเปลี่ยนแปลงที่ทำใน Excel

เพิ่มชีต `รายการดรอปดาวน์` เพื่อรวมตัวเลือก dropdown ทุกช่องไว้ที่เดียว

dropdown ในชีต `เพิ่มข้อมูล` ที่แก้ให้ดึงจาก `รายการดรอปดาวน์`:

- `C5` ปี/ฤดูกาล
- `C6` สินค้า
- `C7` ประเภทรายการ
- `C8` สายพันธุ์/เกรด
- `C9` ชื่อ/รายการ
- `F6` วิธีรับ/จ่ายเงิน
- `F7` สถานะรายการ

dropdown ในชีต `สรุป`:

- `B3` ปี/ฤดูกาล
- `D3` เดือน

เพิ่มปี:

- `2569-2580`

เพิ่มตัวเลือกสายพันธุ์/เกรด:

- `เกรด1`
- `เกรด2`
- `เกรด3`
- `เกรด4`
- `เกรดพุ่ม`
- `เกรดปุก`
- `เกรดดำ`
- `เกรดร่วง`

## วิธีเพิ่ม dropdown ในอนาคต

1. เปิดชีต `รายการดรอปดาวน์`
2. เพิ่มค่าต่อท้ายคอลัมน์ที่ต้องการ
3. กลับไปใช้ dropdown ในชีต `เพิ่มข้อมูล` หรือ `สรุป`

## Apps Script สำหรับบันทึกฟอร์มเข้าฐานข้อมูล

แนวคิด:

- ใช้ช่อง `F14` ในชีต `เพิ่มข้อมูล` เป็นสถานะตรวจสอบ
- เมื่อเลือก `เสร็จเรียบร้อย`
- สคริปต์จะเอาข้อมูลจาก `เพิ่มข้อมูล!A18:R18`
- ไปต่อท้ายชีต `ฐานข้อมูล`
- แล้วล้างฟอร์มกลับเป็นค่าเริ่มต้น
- ตั้ง `F14` กลับเป็น `ยังไม่เสร็จ`

โค้ดหลักที่ใช้:

```javascript
function onEdit(e) {
  if (!e || !e.range) return;

  const sheet = e.range.getSheet();
  if (sheet.getName() !== "เพิ่มข้อมูล") return;

  if (e.range.getA1Notation() !== "F14") return;

  const status = e.range.getDisplayValue();
  if (status !== "เสร็จเรียบร้อย") return;

  appendFormToDatabase_();
}

function appendFormToDatabase_() {
  const lock = LockService.getScriptLock();
  if (!lock.tryLock(5000)) return;

  try {
    const ss = SpreadsheetApp.getActiveSpreadsheet();
    const form = ss.getSheetByName("เพิ่มข้อมูล");
    const db = ss.getSheetByName("ฐานข้อมูล");

    const status = form.getRange("F14").getDisplayValue();
    if (status !== "เสร็จเรียบร้อย") return;

    const values = form.getRange("A18:R18").getValues()[0];
    if (values.every(v => v === "")) return;

    const nextRow = Math.max(db.getLastRow() + 1, 5);
    db.getRange(nextRow, 1, 1, values.length).setValues([values]);

    form.getRange("C4").setValue(new Date());
    form.getRange("C5").setValue(2569);
    form.getRange("C6:C11").clearContent();

    form.getRange("F4").setValue(0);
    form.getRange("F5").setValue(0);
    form.getRange("F6").clearContent();
    form.getRange("F7").setValue("ปกติ");
    form.getRange("F8").clearContent();

    form.getRange("F14").setValue("ยังไม่เสร็จ");

    ss.toast("บันทึกเข้าฐานข้อมูลแล้ว และล้างฟอร์มเรียบร้อย", "สำเร็จ", 3);
  } finally {
    lock.releaseLock();
  }
}
```

## สรุปแยกวิธีรับ/จ่ายเงิน

ผู้ใช้ต้องการเพิ่มในชีต `สรุป`:

- เงินสด
- เงินโอน
- ค้างรับ
- ค้างจ่าย

โดยให้กรองตามปี `B3` และเดือน `D3` เหมือนตารางสรุปหลัก

วิธีเพิ่มใน Google Sheets เดิม:

1. เปิด Apps Script
2. วางฟังก์ชัน `setupPaymentSummary`
3. กด Save
4. เลือกฟังก์ชัน `setupPaymentSummary`
5. กด Run
6. อนุญาตสิทธิ์ครั้งแรกถ้า Google ขอ

หมายเหตุ: ไม่ต้อง Deploy

## เรื่องสิทธิ์ Apps Script

ถ้าขึ้นกล่อง `จำเป็นต้องให้สิทธิ์`:

1. กด `ตรวจสอบสิทธิ์`
2. เลือกบัญชี Google
3. ถ้าขึ้นแอปไม่ได้รับการยืนยัน ให้กด `ขั้นสูง`
4. กด `ไปที่โครงการไม่มีชื่อ`
5. กด `อนุญาต`
6. กลับมากด Run อีกครั้ง

## หมายเหตุเรื่องปี

ถ้าเลือกปี `2570` ในชีต `สรุป` สูตรรายเดือนจะดูวันที่ปี ค.ศ. `2027` เพราะ `2570 - 543 = 2027`

ดังนั้นข้อมูลที่กรอกปี/ฤดูกาล `2570` ควรมีวันที่เป็นปี `2027` เช่น `27/5/2027` เพื่อให้รายเดือนขึ้น

## เว็บแดชบอร์ด

สร้างโฟลเดอร์เว็บ:

`C:\Users\sippk\OneDrive\เอกสาร\ทำเว็ปเพจ\แดชบอร์ด\web-dashboard`

ไฟล์หลัก:

- `index.html`
- `styles.css`
- `app.js`
- `README.md`

เว็บดึงข้อมูลจาก Google Sheets ล่าสุดผ่าน Google Visualization endpoint และอ่านชีต `ฐานข้อมูล`

ฟีเจอร์ที่ทำแล้ว:

- KPI รายรับ รายจ่าย สุทธิ ปริมาณ ราคาเฉลี่ย และเงินค้าง
- ข้อความสรุปอัตโนมัติ
- ตัวกรองปี เดือน สินค้า วิธีรับ/จ่ายเงิน สถานะ
- ตัวกรองสายพันธุ์/เกรด
- เว็บพยายามอ่านค่า dropdown จากชีต `รายการดรอปดาวน์` เพื่อจำตัวเลือกไว้ล่วงหน้า แม้บางค่ายังไม่มีข้อมูลใน `ฐานข้อมูล`
- filter chips
- donut chart เลือก metric ได้: รายรับ/รายจ่าย/สุทธิ
- donut chart ปรับเป็น nested donut: วงในเป็นสินค้า วงนอกเป็นสายพันธุ์/เกรดของสินค้าแต่ละชนิด
- ชนิดสินค้าหลักใน donut/filter แสดงเสมอ: ทุเรียน, ลองกอง, มังคุด
- เกรดหลักแสดงเสมอ: เกรด1, เกรด2, เกรด3, เกรด4, เกรดพุ่ม, เกรดปุก, เกรดดำ, เกรดร่วง
- เมื่อเลือกสินค้าแล้ว ตัวกรองสายพันธุ์/เกรดจะใช้ตรวจข้อมูลจริงใน `ฐานข้อมูล`; ถ้าไม่มีรายการตรงกัน ค่า KPI/กราฟ/ตารางจะแสดงเป็น 0 หรือว่างตามจริง
- คลิก donut เพื่อกรองสินค้า
- donut รองรับกรณีมีข้อมูลชนิดเดียว โดยวาดเป็นวงเต็ม
- วิธีรับ/จ่ายเงินในเว็บรวม `โอน` เป็น `เงินโอน` และซ่อน `โอน` ไม่ให้เป็นตัวเลือกแยก

ปรับล่าสุด:

- ใส่ภาพพื้นหลังจากไฟล์ `hero-bg.jpg.png` เป็น `web-dashboard/assets/fruit-bg.png`
- พื้นหลังใช้ภาพผลไม้จริงพร้อม overlay เขียวเข้ม เพื่อให้อ่านตัวหนังสือง่าย
- ปุ่ม `ล้างตัวกรอง` รีเซ็ตค่า UI ทุกช่องกลับตามค่าเริ่มต้นจริง
- ตัวกรอง `สายพันธุ์/เกรด` อ่านรายการจากชีต `รายการดรอปดาวน์` และมีค่าเกรดหลักสำรองไว้ในเว็บ

## GitHub Publishing Plan

สถานะปัจจุบัน:

- โฟลเดอร์นี้ยังไม่ได้เป็น git repository ตอนตรวจครั้งแรก
- เครื่องนี้ยังไม่มีคำสั่ง `gh` จาก GitHub CLI
- สร้างไฟล์ `README.md` สำหรับหน้า repo แล้ว
- สร้างไฟล์ `.gitignore` เพื่อกันไฟล์ Excel, cache, และ preview ชั่วคราวไม่ให้ขึ้น GitHub

ขอบเขตที่ควรอัปขึ้น GitHub:

- `README.md`
- `web-dashboard/index.html`
- `web-dashboard/styles.css`
- `web-dashboard/app.js`
- `web-dashboard/assets/fruit-bg.png`
- `web-dashboard/README.md`
- `เก็บข้อมูลเบื้องลึกDashBoard/*.md`

ไฟล์ที่ไม่ควรอัปเป็นค่าเริ่มต้น:

- ไฟล์ `.xlsx`
- `__pycache__/`
- preview screenshot ที่ regenerate ได้

สิ่งที่ต้องถามผู้ใช้ก่อน push:

- ชื่อ repo บน GitHub
- ต้องการ public หรือ private
- จะให้ใช้ GitHub CLI หรือให้ผู้ใช้สร้าง repo เปล่าบนเว็บ GitHub แล้วส่ง URL remote มา
- กราฟรายเดือน คลิกเดือนเพื่อกรอง
- กล่องเงินสด เงินโอน ค้างรับ ค้างจ่าย คลิกเพื่อกรอง
- อันดับสินค้า รายจ่าย และเกรด
- ตารางรายการต้องติดตาม
- ตารางรายการล่าสุด
- responsive สำหรับมือถือ

URL local ที่ใช้ทดสอบ:

`http://127.0.0.1:8765/`
